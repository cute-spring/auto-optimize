# FAQ Retrieval Example

This example provides a minimal contract and workspace fixture for validation.

Run:

```bash
python -m auto_optimize.cli validate examples/faq_retrieval/optimization.contract.yaml
```
